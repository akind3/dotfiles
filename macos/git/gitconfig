# This is Git's per-user configuration file.
[user]
	name = akind3
	email = akinthsen@gmail.com
# Please adapt and uncomment the following lines:
#	name = akin
#	email = akin@x1.local
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[log]
  date = iso-local
